<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <style>

    body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #000000;
    }
    
    h1 {
        color: white;
        text-align: center;
    }
    
    /* Navbar Styling */
    .navbar {
        height: 35px;
        background-color: #333;
        color: white;
        padding: 15px;
        position: relative;
    }
    
    .navbar ul {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
    }
    
    .navbar ul li {
        margin: 0 15px;
    }
    
    .navbar ul li a {
        color: white;
        text-decoration: none;
        font-weight: bold;
    }
    
    .navbar ul li a:hover {
        text-decoration: underline;
    }
    
    /* Hamburger Menu Styling */
    .hamburger {
        display: none;
        flex-direction: column;
        cursor: pointer;
        position: absolute;
        top: 20px;
        right: 20px;
    }
    
    .hamburger div {
        width: 25px;
        height: 3px;
        background-color: white;
        margin: 4px 0;
    }
    
    /* Hide Navbar Menu for Mobile */
    .navbar ul {
        flex-direction: row;
    }
    
    .navbar ul.show-menu {
        display: flex;
        flex-direction: column;
        position: absolute;
        top: 60px;
        right: 0;
        background-color: #333;
        width: 100%;
    }
    
    .navbar ul.show-menu li {
        text-align: center;
        margin: 10px 0;
    }
    
    .form-container {
        background-color: white;
        margin: auto;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 300px;
        margin-top: 10px;
        height: 45vh;
    }
    .form-container h2 {
        text-align: center;
        margin-bottom: 20px;
    }
    .form-group {
        margin-bottom: 15px;
    }
    .form-group label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
    }
    .form-group input {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
    }
    .form-group input:focus {
        border-color: #007bff;
    }
    .form-btn {
        width: 100%;
        padding: 10px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }
    .form-btn:hover {
        background-color: #0056b3;
    }
    a .forgetPassword{
        color: blue;
        background: red;
    }
        /* Media Query for smaller screens */
@media (max-width: 768px) {
    .hamburger {
        display: flex;
    }

    .navbar ul {
        display: none;
    }

    .navbar ul.show-menu {
        display: flex;
    }
}
</style>

    
</head>
<body>
    <%- include("components/navbar.ejs") %>
    <div class="form-container">
        <div class="flash-message-container" style="display: flex; justify-content: center; align-items: center; height: auto; background-color: rgba(255, 255, 255, 0.9);">
            <p id="flash-message" style="color: red; font-size: medium; font-weight: bold; margin: 0;"><%- error %></p>
        </div>
                <h2>Login</h2>
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit" class="form-btn">Login</button>
        </form>
        <div style="display: flex; justify-content: center; align-items: center; height: 10vh;">
            <a href="/forgetPassword" style="color: white; background: red; text-decoration: none; padding: 10px; border-radius: 10px; text-align: center; font-size: 11px">Forget Password</a>
        </div>
    </div>
    <%- include("components/footer.ejs") %>

</body>
<script>
    function toggleMenu() {
        const navbarMenu = document.getElementById('navbar-menu');
        if (navbarMenu.classList.contains('show-menu')) {
            navbarMenu.classList.remove('show-menu');
        } else {
            navbarMenu.classList.add('show-menu');
        }
    }
    setTimeout(function() {
        const messageContainer = document.querySelector('.flash-message-container');
        messageContainer.style.display = 'none'; // Hide the message
    }, 2000); // 2000 milliseconds = 2 seconds
</script>
</html>
